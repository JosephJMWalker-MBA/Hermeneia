# Hermeneutic Field

The Field organizes relationships among ontology objects.

It never creates observations. It computes structure over existing ontology.

Responsibilities:
- graph traversal
- coverage analysis
- perspective debt
- meaning pressure
- neighborhood queries

Forbidden:
- LLM calls
- narrative generation
- ontology mutation
