# Compiler

The compiler transforms external corpora into immutable ontology objects.

It performs parsing and structural decomposition only.

Forbidden:
- LLM calls
- interpretation
- sentiment
- metaphor detection
- narrative generation
